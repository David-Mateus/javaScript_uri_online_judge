var line = lines.shift().split(" ");
let chico = 300 * parseInt(lines.shift().split());
let bento = 1500 * parseInt(lines.shift().split());
let bernardo = 600 * parseInt(lines.shift().split());
let marina = 1000 * parseInt(lines.shift().split());
let iara = 150 * parseInt(lines.shift().split());
let marlene = 225;
let total = (chico + bento + bernardo + marina + iara + marlene); // Digite aqui o calculo total
console.log(total);